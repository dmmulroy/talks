// Generated by Melange

import * as App__Open_ai from "./open_ai.mjs";
import * as Curry from "melange.js/curry.mjs";
import * as Ai from "ai";

function let$star(t, f) {
  return t.then(Curry.__1(f));
}

function main(article) {
  const t = Ai.generateText({
        model: App__Open_ai.model(/* Gpt_4o */1),
        system: "You are a professional writer. You write simple, clear, and concise content.",
        prompt: "Summarize the following article in 3-5 sentences: " + article
      });
  return t.then(function (param) {
              console.log(param.text);
              return Promise.resolve(undefined);
            });
}

export {
  let$star ,
  main ,
}
/* App__Open_ai Not a pure module */
